import SwiftUI

/// Стаб экрана «Перевод». Реализация — Dev 2, ветка feature/transfer.
/// Имя типа зафиксировано: на него ссылается RootTabView.
struct TransferScreen: View {
    var body: some View {
        Text("Перевод — экран в разработке")
    }
}
