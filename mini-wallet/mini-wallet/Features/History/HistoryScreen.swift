import SwiftUI

/// Стаб экрана «История». Реализация — Dev 3, ветка feature/history.
/// Имя типа зафиксировано: на него ссылается RootTabView.
struct HistoryScreen: View {
    var body: some View {
        Text("История — экран в разработке")
    }
}
