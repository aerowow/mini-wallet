import SwiftUI

/// Стаб экрана «Счета». Реализация — Dev 1, ветка feature/accounts.
/// Имя типа зафиксировано: на него ссылается RootTabView.
struct AccountsScreen: View {
    var body: some View {
        Text("Счета — экран в разработке")
    }
}
