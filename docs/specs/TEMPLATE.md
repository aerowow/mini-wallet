# ТЗ: <название экрана>

**Автор:** PM · **Исполнитель:** Dev N · **Ветка:** feature/<...> · **Статус:** draft / согласовано

## 1. Назначение экрана
<!-- Одним абзацем: что видит пользователь и зачем. -->

## 2. Данные из общего контракта
<!-- Перечислить, какие модели и методы контракта использует экран.
     Контракт создаётся командой отдельным PR и после мержа заморожен;
     если экрану нужно что-то сверх контракта — фиксировать здесь и выносить в чат. -->

## 3. UI и состояния
<!-- Список элементов сверху вниз. Обязательно описать состояния:
     обычное, пустое, ошибка (для перевода — все случаи ошибок и повтор-дубликат). -->

## 4. Критерии приёмки
<!-- Проверяемые пункты «дано/когда/тогда», по которым делается ревью PR. Например:
     - Дано: перевод выполнен с ключом K. Когда: повтор с тем же K.
       Тогда: показано «дубликат», балансы не изменились, новой транзакции нет. -->

## 5. Вне скоупа
<!-- Что осознанно НЕ делаем (анимации, локализация, лендскейп и т.п.). -->
